{
  "$id": "https://gitlab.com/boonlogic/development/mongolia/mongolia/test/tenant.json",
  "type": "object",
  "required": [
    "tenantId",
    "name"
  ],
  "x-unique": [
    "tenantId",
    "name"
  ],
  "properties": {
    "tenantId": {
      "type": "string",
      "pattern": "^[0-9a-z]{24}$"
    },
    "name": {
      "type": "string",
      "pattern": "^[a-zA-Z0-9-_]*$",
      "maxLength": 64
    }
  }
}
